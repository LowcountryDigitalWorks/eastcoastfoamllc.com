---
import '../styles/global.css';
import '../styles/visual-pass.css';
import '../styles/classical-parity.css';
import '../styles/classical-pages.css';
import '../styles/classical-fidelity.css';
import '../styles/classical-fidelity-04.css';
import '../styles/classical-fidelity-09.css';
import '../styles/classical-mobile-polish.css';

interface Props {
  title: string;
  description: string;
  theme?: 'chooser' | 'classical' | 'future';
}

const { title, description, theme = 'chooser' } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="robots" content="noindex,nofollow,noarchive,nosnippet" />
    <meta name="googlebot" content="noindex,nofollow,noarchive,nosnippet" />
    <meta name="theme-color" content={theme === 'classical' ? '#111111' : '#102a20'} />
    <title>{title}</title>
  </head>
  <body class={`theme-${theme}`}>
    <slot />
  </body>
</html>
